o # Language Model Learning Dependencies
# Install with: pip install -r requirements.txt

# Core PyTorch for neural networks
torch>=2.0.0

# NumPy for numerical operations
numpy>=1.24.0

# Matplotlib for plotting training curves
matplotlib>=3.7.0

# Collections module (built-in, but good to note)
# re module (built-in regex)

# Optional: For better progress bars during training
tqdm>=4.65.0

# Optional: For Jupyter notebook support (if you want to experiment)
jupyter>=1.0.0
ipython>=8.0.0

# Optional: For saving/loading models
pickle-mixin>=1.0.2
